subprojects {
    plugins.withType(com.android.build.gradle.BasePlugin) {
        project.android {
            compileSdkVersion 34
            defaultConfig {
                targetSdkVersion 34
            }
        }
    }
}
